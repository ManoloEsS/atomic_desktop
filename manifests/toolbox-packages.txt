# Minimal DNF set inside the fedora-desktop-dev Toolbx.
# Mise cannot distribute these reliably, so they stay native.
# Everything else personal/CLI is managed by Mise.
git
openssh-clients
neovim
# Terminfo for Ghostty (TERM=xterm-ghostty); without it every TUI breaks.
ncurses-term
